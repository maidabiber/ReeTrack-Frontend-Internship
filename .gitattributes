# Deployment files are consumed on Linux (Docker RUN, nginx). Force LF so a Windows
# checkout can't inject CRLF and break them.
*.sh          text eol=lf
Dockerfile    text eol=lf
*.conf        text eol=lf
*.yml         text eol=lf
*.yaml        text eol=lf
.dockerignore text eol=lf
